// main.js

// Link the necessary functions
document.addEventListener('DOMContentLoaded', () => {
  // Initialize the settings menu
  initializeSettings();

  // Initialize the cosmetics menu
  loadCosmetics();

  // Initialize the game
  initializeGame();
});
